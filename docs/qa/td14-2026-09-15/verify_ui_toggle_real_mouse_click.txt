T-D14 追加实机验证：用真实鼠标点击暂停菜单开关（2026-09-15 深夜）
==============================================================

目的：补上验收 #3（可开关的设置项）端到端证据——此前的 OFF 对照是「重新构建时把开关初值改 false」，
      本次改为在运行时用真实鼠标点击按钮翻转，不再依赖重新构建。

工具：qa_drive.mm 新增 pauseclick 模式（同进程内完成：激活 → HID 注入 ESC 开菜单 → 鼠标移到按钮
      (960,453) 全局屏幕点 → 左键按下 150 ms → 抬起 → 截图）。坐标换算：窗口原点 (320,89) +
      按钮中心 (640,374) 帧缓冲像素（本机 1x，点==像素）。

① 点击前：按钮标签为 AUTO-JUMP ON（默认值；构建时开关初值 true）
② 真实鼠标点击后：shot_ui_click_auto_jump_off.png —— 菜单显示 PAUSED / RESUME / AUTO-JUMP OFF / QUIT
   ⇒ 点击确实翻转了内存态开关，标签实时反映状态
③ 点 RESUME 恢复（clickresume 模式，也是真实鼠标点击）后注入 W：
   逐帧 trace 全程 inj=0（自动化注入计数 0），玩家被 1 格石墙夹停在 z=0.3、y 恒 132.0000：
      EV t=3073 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
      EV t=3074 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
      EV t=3075 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
      EV t=3076 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
      EV t=3077 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
      EV t=3078 x=2.7000 y=132.0000 z=0.3000 og=1 vy=0.0000 inj=0
   （该 run 的测试墙由临时 harness 放在出生点正前方 1 格；代码见 temporary_harness.patch）
   ⇒ 由 UI 关闭后，功能确实停止工作，与「重新构建关闭」结论一致，且不需要重新构建

注：同一次运行中，鼠标移动事件使视角发生过偏转（玩家 x 从 0.5 漂到 2.7），属注入鼠标的副作用，
    不影响上述结论；墙在 x 方向连续 5 格，仍构成有效阻挡。
④ 全程 inj=1 计数 = 0；y 取值集合 = {132.0000}
