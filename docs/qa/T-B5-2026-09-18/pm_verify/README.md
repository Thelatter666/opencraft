# T-B5 · PM 侧验收装置与证据

> 日期：2026-09-18　执行：PM（第四任）　裁决：`docs/tasks/T-B5.ruling.md`
> 对象：分支 `task/T-B5-mob-art-rework` @ `a2711c7`（基线 = main `ba2f920`）→ merge 见裁决

## 1. 干净检出与回归（★ 本轮新增离线法）

`git archive a2711c7` 自行 configure/build → **ctest 459/459**（纯资产卡）。
⚠ 本轮**外网全断**（代理 7890 拒连、直连超时）⇒ 用
**`FETCHCONTENT_SOURCE_DIR_*` 指向主仓 `build/_deps/*-src` 的只读拷贝**（tar 排除 .git，52 MB）
完成 configure——**不动主仓、不联网**，命令与变量名见本目录 `README` 与本回合提交说明。
（开发者自身构建早于断网，不受影响。）

## 2. 产品码日志（无补丁）

`pm_run_with_assets.log`：`atlas: 60/63` + `mobs: 2/3` +
`mob model mossback: 705 voxels, 1544 triangles, 7 joints, 1.4 blocks tall, palette from …` +
`mob model hollow_wretch: 496 voxels, 1592 triangles, 6 joints, 1.8 blocks tall, palette from …`，
`[warning]/[error]` = 0；`pm_run_without_mobs.log`：`0/3`、0。

## 3. 第四份独立实现读回（`pm_readback_b5.py`，**ALL PASS**）

两件全项：md5 四值与报告同（`fda9ee82…`/`2be25d95…`/`2af82806…`/`264c75d7…`）；
SIZE/体素数（705/496）；每索引计数与包围盒；**专项**：
- Mossback：**腿 z 高 = 7（0..6）**；**四腿与躯干外缘齐平**（legs x {0,1,5,6} vs torso x 0..6）；
  **苔顶 13 = 头 max z 13**（不再"苔比头高"）；z=0 层只含腿关节 {2,3,4,5}；面格 5 个
  （两 2×1 眼窝 + 鼻点）全部"在占用表内 + `+y` 邻居为空"且在头关节包围盒内。
- Wretch：**四肢各 2 格宽**（arm x1..2/9..10、leg x4..5/6..7）；**面部 14 格**（2×2 双眼窝 +
  鼻窝 2 + 口 4）同为表面着色；z=0 层只含腿关节 {4,5}；第二色（10/13/14）各自落在其关节包围盒内。

## 4. 暂停帧实机（装置 = rev2 + **本回合新增"相机同位校验"**）

★ 本回合新踩的坑：**HID 激活会让相机 yaw 随机漂 ±2px**（同一场景两跑差 16.8 万像素、整屏 bbox）。
修法 = `pair.sh`：拍一对后用**无生物区（左 300px）**做 (dx,dy) 对齐检测，**只有 ratio=1.0 且
dx=dy=0 才接受**，否则重拍（本轮第 4 次命中）。旧口径"同平台门"不能替代这一条。

| 比对 | 结果 | 判定 |
|---|---|---|
| 苔背兽 模型 vs 模型（两跑） | **0 / 957,440** | 噪声地板仍为 0（相机同位已验） |
| 苔背兽 模型 vs 占位 | **20,294**（紧邻生物区域的色斑=苔绿/木棕） | v5 上屏替换占位 |
| 枯役 模型 vs 占位（同位对） | **19,834**（x578..701, y365..585） | 同上；占位主色 (51,51,53)/(44,44,46)=灰盒 |

目检：`wr_model_zoom.png`（v5 枯役实机：**胸腔空洞 + 双臂骨手 + 双腿**清楚；头顶按几何进菜单带）、
`wr_standin_zoom.png`（占位灰盒）、`n_m1_zoom.png`（v5 苔背兽实机：**脸在游戏里可读**
——棕色吻部上两只黑眼窝 + 鼻点）、`face_wretch_zoom.png`（**暂停前俯仰单张**：
**2×2 眼窝 + 鼻 + 口全部可见** ⇒ 用户"必须有明显面部特征"在引擎内成立）。
其余原始 session 日志 6 份同目录。

> 口径提醒：俯仰注入**单张可用、跨跑不可复现**（本会话实测每跑落点不同）⇒ 它只用于目检照，
> **不进 A/B 矩阵**；A/B 一律 pitch=0 + 相机同位校验。
