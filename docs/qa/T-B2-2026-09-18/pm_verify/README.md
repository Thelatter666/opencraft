# T-B2 · PM 侧验收装置运行（在 T-B1 pm_verify rev2 之上）

> 日期：2026-09-18　执行：PM（第四任）
> 装置：`docs/qa/T-B1-2026-09-18/pm_verify/` 的 rev2 暂停帧装置原样复用
> （补丁 01/02/03 + `run_scene.sh`）；**唯一改动**：取证树里把召唤点
> `position + facing * 2.0` 调到 `* 4.5`（见 §3 的几何账）。
> `run_scene_pitch.sh` = 加了"召唤后注入俯仰"实验步的副本（该路线实测无效，§4）。

## 1. 干净检出与回归

`git archive 321c362 | tar -x` → 自行 configure（未设 `FETCHCONTENT_BASE_DIR`）→ 构建 →
**ctest 457/457**。合入后主树再跑 457/457。代码与 main 逐字节相同（白名单内零代码，
`git diff --name-only main...branch` 全为资产/证据/登记文件），故构建警告集对照免做。

## 2. 产品码日志判据（无补丁，本目录三份 log）

| 跑法 | 结果 |
|---|---|
| 有 `assets/mobs/mossback.vox` | `mobs: 1/3` + `mob model mossback: 672 voxels, 1504 triangles, 7 joints, 1.4 blocks tall, palette from palettes/mossback.png`，**WARN=0**（`with_model.log`，与开发者行逐字同，除时间戳） |
| 移走 `assets/mobs/` | `mobs: 0/3`，**WARN=0**（静默，`without_model.log`） |
| 额外探针：只移走调色板 PNG | `1/3` 且行尾**没有** `palette from`（回落到 .vox 自带 RGBA），WARN=0（`no_palette_png.log`）——契约 ④ 的"PNG 接管/缺失回落"两向都通 |

二进制内 `strings | grep -c EVIDENCE = 0` ⇒ 日志判据跑的是产品码。

## 3. 暂停帧逐像素（取证树 = 321c362 + 01/02/03 补丁）

全部场景：平台门 `chunks=113 meshed=104` 同读数、`stable=1`、`natural=0`。
比对工具 = `docs/qa/T-B1-2026-09-18/tools/pixel_diff.py`（解码逐像素）。

| # | 比对 | 召唤距离 | 差异像素 | 判定 |
|---|---|---|---|---|
| [1] | m1 vs m2（同树同资产两跑） | 2.0 格 | **0 / 957,440** | 复现性=噪声地板 0 |
| [2] | m1 vs s1（模型 vs 两立方体） | 2.0 格 | **84,827**（x491..807, y432..747） | 模型替换占位，色差=木棕+苔绿 |
| [3] | m1 vs c1（有生物 vs 空） | 2.0 格 | **72,111** | 灵敏度 |
| [4] | s1 vs c1（两立方体 vs 空） | 2.0 格 | **86,080** | 对照组健全 |
| [5] | m3 vs c1 | 5.0 格 | 14,110（x584..702, y409..573） | 头顶被暂停菜单截 ~45 px |
| [6] | m3 vs s2 | 5.0 格 | 16,169 | 同上 |
| [7] | **m7 vs c1（定稿）** | **4.5 格** | **17,641** | 全身完整落在菜单下缘与热栏上缘之间 |
| [8] | **m7 vs s4（定稿对照）** | **4.5 格** | **20,496** | 同一框位：完整动物 vs 两立方体 |

目检图：`pm_final_m7_mob.png`（3× 最近邻放大：**浅木色头在左、四条深色腿、背/右侧深绿苔丘、
单一体块连成一只**）与 `pm_final_s4_standin.png`（同框位两立方体，接缝清晰）。
`pm_zoom2_m3.png` 为 5.0 格存档。⇒ **契约 ⑥"画面里它真的不再是两个立方体"由 PM 侧补齐**。

### 距离账（写给下一个高个子模型）
暂停菜单带固定在屏幕中部（底缘 y≈410/748），热栏顶缘 y≈690；眼睛高 1.62、
碰撞高 1.4 的生物**全身投影高度**≈ `atan(1.62/d)..atan(0.22/d)`，
d=5 顶缘 409（差 1 像素被截）、d=4.5 顶缘 ≈416 恰好躲开。**1.4 格高的生物用 d=4.5。**

## 4. 两条失败路线（如实留痕，别重走）

- **横移生成点**：`+2 右` 落在一格高的草阶上，生物整只埋进坡里（m4：vs c1 仅 127 px，
  只露耳朵）；再 `+1.2 上` 只露出苔丘一角（m5：6,368 px）。**横向列地形不受控，别挪。**
- **暂停态注入鼠标俯仰**（`ti2input move 0 -40`）：**画面与不注入逐像素相同**
  （m6 vs c1 = m5 vs c1 = 6,368，bbox 一模一样）——暂停时鼠标未捕获、
  视角回调不吃 delta。**俯仰路线在暂停帧装置里是死的。**

## 5. 第三份独立实现读回（`pm_readback.py`，不 import 开发方任何脚本）

对合入件 `assets/mobs/mossback.vox` + `assets/palettes/mossback.png` 全项 PASS：
SIZE 9×14×14、672 体素、XYZI 声明诚实、colorIndex 0 未用、关节标签 {1..7}、
躯干组 {9..16}、每标签包围盒与报告 §3 表逐项一致、z=0 层只含 {3,4,5,6}（抗 y-up 判据）、
头 z 高于躯干标签格（11>9）、RGBA 256 条且"第 e 条=索引 e+1"逐槽验证、
PNG 格 1..16 与 RGBA 逐格相同、alpha 全 255、12 色全在 §2.1、最大饱和 0.471。

## 6. 可复现性

`vox_build.py <scratch>` 重跑 → 两个资产与提交件 **md5 逐字节相同**
（`f8072830088c024aa4ad724465a1b99b` / `ba69f5a0ade80bb44fc0291fe3c9f5b9`，与报告宣称一致）。
