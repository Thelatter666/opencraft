# T-A4 裁决（2026-09-18）

> 依据：美术总监报告 `docs/tasks/T-A4.report.md` + **PM 独立复现**
> （美术类产出不能靠单测，`docs/05 §3.1` 第 5 条）。

---

## §0 PM 独立复现

### 干净检出（新建 `/tmp/ta4_verify`，从零 configure+build）

| 项 | 结果 |
|---|---|
| ctest | **438/438** ✓ |
| **白名单外已跟踪文件** | **零改动** ✓ |
| **过程垃圾**（`__pycache__`/`*.pyc`） | **无** ✓（本卡吸取了 T-A3 教训） |

### ★ 日志判据（PM 自跑 dumper）

```
atlas: 60/63 block tiles loaded from assets/blocks
```

### ★ 反解核对（沿用 T-A3 那套，能抓出"文件放错方块"）

把变化的 60 个瓦片按 `tile = block_id × 3 + slot` 反解：

```
变化瓦片 60 / 81 ；覆盖方块 20
第二批 6 个：cobblestone / gravel / sandstone / bedrock / snow_block / obsidian
            —— 各 top+side+bottom ✓
其余 14 个（T-A3 已做）：仍各三面 ✓
三面不齐全的：无 ✓
```

⇒ **20 方块 × 3 面 = 60，与交付清单完全一致、无错放。**

### 逐文件规格

```
正好 16×16：60 / 60，不合格 0 ✓
```

### CREDITS 交叉核对

```
文件 60 ⇄ CREDITS 条目 60，缺 0、多 0 ✓
六字段齐全、无空列：0 处不合格 ✓
```

### 规格 §9

已标记「★ T-A4 已完成」，并追加登记项。正文其他节未动 ✓（与 T-A3 做法一致）。

---

## 验收结论：**通过**（卡面 §6 九项全达成）

---

## ★ 最重要的一条：他自曝了"两张贴图玩家看不到"

**已知问题 4**：`cobblestone` / `sandstone` —— ① 不在 worldgen 生成
② 其物品 `rubble_rock` / `grit_slab` 不在启动包 ⇒ **正常游戏里看不到这两张新贴图**。

### PM 独立核实：**属实**

```
worldgen 生成：grep cobblestone|sandstone → 零命中（不在世界生成里）
启动 hotbar：greyrock/sod_loam/sawn_planks/duskglass/vessel×2/grain_loaf/
             timber_chisel/rime_block  —— 无 rubble_rock / grit_slab
启动主库存：char_ore/ferrous_ore/lucent_ore/sunroot/timber_* —— 同样没有
掉落表：零命中
全仓：这两个 id 只出现在 item_registry.cpp 的注册行，无任何获得途径
```

⇒ **这两张贴图（6 个文件）目前是"已落盘但不可达"的。**

**这不是缺陷，是内容缺口**——与 T-D17 那次同类（当时 PM 误判为适配器缺陷，
真因是内容不存在）。**他没有掩盖，是主动报出来的**，处置正确。

**裁决**：
- **不因此打回本卡**——贴图本身合格，缺的是"获得途径"，属另一张卡的职责；
- 登记 **T-D53**（补 `rubble_rock`/`grit_slab` 的获得途径），否则玩家永远看不到这两张；
- 同时在裁决里留一句提醒：**"资产已落盘" ≠ "玩家能看见"**，后续美术卡凡做
  "世界里不自然生成的方块"，须先确认可达性。

---

## 他的装置：★ 展示台 + 对照组

他指出：**6 个方块里 4 个在出生点视野里根本不存在**，且"文件名写错 = 静默回退"，
所以 `60/63` 只能证明"文件被读进去了"，**不能证明"上屏的是这 6 张"**。

他的解法：
1. 改写 `build/saves` 里**游戏自己写出的存档**造展示台（T-D4 卡面授权过的同类手法），
   **产品代码零改动**；
2. **加了一个"没变"的对照组**（stone 石台 0.0% 变化）——证明差异只来自这 6 张，
   而不是别的贴图也变了或被误读。
3. 99.4% 的变化像素落在「色板色 × 引擎面明暗」±24 内 ⇒ 上屏的确实是色板色。

**裁决：装置设计合格，且第 2 点（对照组）是本卡方法论的关键贡献。**

⇒ **当场落实**：把这套判据写进 `docs/05 §3.1`（见下方）。

---

## 其他已知问题处置

| # | 项 | 处置 |
|---|---|---|
| 1 | 黑曜石紫调反光 `{74,62,96}` 不在 32 色板内，改用中性灰 S3，已在 §9 登记"色板待扩" | **接受**。他在白名单限制内（只许改 §9）做了正确处置：不改规格、只登记 |
| 2 | §4.1 对"无方向均匀面"材质（雪）两条规则互相拉扯，用手排 2px 短痕解掉并登记"规则待裁决" | **接受**。如实登记规则冲突，未悄悄绕过 |
| 3 | sandstone 与 sand 同色系，靠簇+短划线+层理区分 | **接受**，记录为观感观察，不自动改卡 |
| 5 | 日志分母 63 含 air 的 3 格，永远到不了 63/63 | **确认属实**（PM 核：21×3=63，air 占 3）⇒ 采纳建议 (a)，登记 **T-D54**（低）：日志分母排除 air |
| 6 | 首轮目检后返工 4 个方块（cobblestone 读成横条纹等） | **记功**：返工依据是 18× 放大自查渲染，且**中间稿未入仓**（在 /tmp）——守住了 T-A3 的过程垃圾教训 |

---

## 他给 PM 的建议 —— 处置

| 建议 | 处置 |
|---|---|
| (a) 日志分母排除 air 的 3 格 | **采纳**，登记 **T-D54**（低） |
| (b) 给 `rubble_rock`/`grit_slab` 一个获得途径 | **采纳**，登记 **T-D53**（中）——否则两张贴图玩家看不到 |
| (c) 沿用 T-A3 的 `is_translucent_block` 硬编码建议（本卡不受影响） | 保持 T-A3 时的既有登记，不重复登记 |
| 方法论：美术卡判据 = 日志 N/63 + 同机位逐像素差异 + **"没变"的对照组** | **采纳，当场写入 `docs/05 §3.1`** |
| 展示台装置改写的是存档、非产品代码 | **认可**。与 T-D4 同类手法；本卡已确认产品代码零改动 |

---

## 当场落实

- [x] `docs/05 §3.1` 新增：美术卡生效判据（日志 + 逐像素 + 对照组；**只看"画面变了"不够**）
- [x] 登记 **T-D53**（`rubble_rock`/`grit_slab` 无获得途径 ⇒ 两张贴图玩家看不到）
- [x] 登记 **T-D54**（启动日志分母含 air 的 3 格，`63/63` 不可达，易误判）
- [x] STATE.md：T-A4 置 done

---

## 记功

1. **★ 主动自曝"两张贴图玩家看不到"**——这是本卡最有价值的一条。
   他本可以只报 `60/63` 就交差；他选择去查"这两张在不在世界里"，
   并给出**可核实的 grep 证据**。这与 T-D17 那次 PM 的误判形成对照：
   **执行方主动做可达性检查，是本项目最缺也最该有的动作。**
2. **装置加了"没变"的对照组**——证明差异只来自目标贴图，排除了"别的也变了"的假象；
3. **返工 4 个方块且中间稿未入仓**——18× 放大自查发现问题就改，且守住了过程垃圾纪律；
4. **在白名单限制内正确处置色板越界**（只登记 §9、不改正文）；
5. **第二批仍未用 AI**（288 行字符网格稿逐格手写 + 自写 PNG 编码器）——
   与首批一致，为美术体系再增加了 18 张无 AI 疑问的资产。

第 1 条尤其重要，值得单独说：**"资产已落盘"不等于"玩家能看见"。**
这条从此进入本项目的验收清单。
