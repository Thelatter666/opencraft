# T-B2b 裁决 · 关节标签扩约 v2 + mossback 资产迁移

> 裁决人：PM（第四任）　日期：2026-09-18
> 对象：分支 `task/T-B2b-joint-expansion` @ `d15bb30`（基线 = main `c5a2839`）→ **合并 `fbef08d`**
> 报告：`docs/tasks/T-B2b.report.md`　PM 证据：`docs/qa/T-B2b-2026-09-18/pm_verify/`
> **结论：通过，合入 `fbef08d`。三条建议逐条见 §2–§4。契约 v2 自合入起生效。**

---

## 0. PM 独立复现（不采信自报）

| 判据 | PM 独立结果 |
|---|---|
| 干净检出回归 | **459/459**；`ctest -N` 用例名集合 `comm` 比对：**存量 457 名零缺失、恰新增 2 个**（合入后主树重建再跑 459/459） |
| 白名单 | 30 文件逐项在卡面 §4 内；`engine/**`、`main.cpp`、`mob_model/render/pose`、CMake、**T-B2 的 QA 目录**全部零改动；CREDITS 仅第 4 列摘要句（两行） |
| 迁移复算 | **PM 第四份实现**（不 import 双方脚本）：坐标序列逐字节同、色字节规则恰为 `{9..16→+8}` 违例 0、RGBA/PNG `9..16=U0 / 17..24=旧 9..16` 逐格违例 0、md5 三值全中、**旧件起点 md5 = T-B2 验收物**；分层稿数据行 diff 空 |
| 日志回归 | `1/3` + 期望行**逐字节同** + WARN=0；移走 `mobs/` → `0/3` WARN=0 |
| **语义生效反证** | 同夹具 `eye_c_second.vox`：v2 二进制 **4 joints** vs v1 二进制 **3 joints** ⇒ 合约变更在真实管线成立（卡面 §7.1"全绿掩盖语义漂移"的警示被这组对照正面拆掉） |
| 警告面 | 8 条 = 已知存量集合（6×`-Wunused-result` + 2×ld duplicate libraries，T-D50）；**改动文件零警告** |
| 第三方 | MagicaVoxel 重开迁移件：形体与迁移前截图逐要素相同（调色板格位按 v2 平移） |
| 引擎 diff | `mob_joint_of_color` 一个函数 + 两处契约注释；测试 diff 删除行 = 两条注释（`VoxVoxel{…,9}` 代码行未动），无 TEST_CASE 删除 |

装置账（PM 自己的坑，留痕）：`pm_migration_check.py` 归档时曾 FAIL——**合并后误把主仓
`assets/` 当"旧件"**；修正 = 一律用分支 qa 归档的 `pre_migration/` 副本并先验 md5。

## 1. 契约 v2（自本合入起生效，B3 起可用）

- `colorIndex 1..16 → 关节 (idx−1) mod 8`（9..16 = 各关节**第二色**）；`17..255 → 躯干组`。
- 关节数上限 8、枢轴、draw call 预算、回退表、解析器、日志判据**均不变**；
  **同关节两色落在同一 part 区段（一次 `glDrawArrays`）**——单测已直接断言。
- mossback 现口径：主色 1..7，躯干色 17..24，9..16 空置（U0）。**B3 人形可用索引 10 画眼睛。**

## 2. 裁决一：卡面 §3.2.2 与 §4 白名单矛盾——**追认"副本迁移"取位**（卡面缺陷，PM 责）

开发者处置正确，且理由比卡面写法更硬：**就地改 T-B2 的 tools 会破坏 T-B2 验收物的 md5 复现链**
（`docs/05` 惯例：历史 QA 证据目录默认不可变）。两条口径当场确认：
① T-B2b 的 tools 副本与原件差异**只有索引分配表与注释**、分层稿数据行逐行相同（PM 已复核）；
② 卡面 §3.2.2 的"就地改"是 PM 写卡失误——已在本卡变更记录追加更正行（第四次卡面自相矛盾类缺陷，
与 T-B2 报告路径案同型），并作为写卡检查项记录。

## 3. 裁决二：存量用例名「…first eight indices」——**保留不改**

"用例名可增不可消失"优先级更高，且该标题在 v2 下**字面仍真**（1..8 确为关节主标签）。
将来若有卡因语义重写而必须换代用例名，须在卡面**逐条 old→new 记账**（本决策不构成例外先例）。

## 4. 裁决三：CREDITS 过程列仍指 T-B2 tools——**维持现状**

过程列的语义是"**创作链/手稿原件**在何处"，那条链的起点（逐层字符稿）确实在 T-B2 目录且
从未被改动；B2b 的迁移副本属"机械变换器"，其存在由两张卡的 QA 目录背书。不改指，避免
"每迁一次改一次 CREDITS"的无谓抖动。

## 5. 通过判定与流程分

- 验收标准 1–5：**全部通过**（含卡面 §5.5 眼睛演示——PM 用双二进制对照加做了一次反证）。
- 流程分：高。三件事是主动做的：① 发现并上报卡面自相矛盾（没有闷声选一条）；② 眼睛演示
  自设"判据放大器"变体（B 版与 A 版逐字相同本身无区分力，C 版才是）；③ 归档 `pre_migration/`
  原始件，使 PM 的独立复算成为可能。
- "扩区间改动存量断言零翻转"自此有实证：**零翻转 + 全绿是预期行为**，区分度必须由新用例
  （或产品路径反证）提供——写进 `docs/05 §3.1` 列表（本提交落地）。
