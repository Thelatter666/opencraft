# 任务 T-R2：M2c 生存规则调研补缺（生物 AI / 配方 / 掉落物 / 经验 / 食物）

里程碑：M2c 前置（调研卡）　前置：**T-I1 / T-I2**（物品与库存已交付）
基线：**324/324**。本卡**不改任何 C++ 代码**。
卡面落盘日期：2026-09-17（PM）

---

## 目标（一段话）

产出一份合规的调研文档 `docs/research/11-mc-survival-systems.md`，
补齐 OpenCraft M2c（生存规则）在**现有文档里缺失**的六项信息，
使其能直接支撑 M2c 的开发卡。

**你是技术顾问与研究员，不是开发者。本卡的产出物是文档，不是代码。**

## 为什么（PM 已逐条核查，缺口是实测的）

PM 于 2026-09-17 逐项核查了 M2c 所需信息 vs 现有文档：

**已覆盖（不必重复调研）**：

| 主题 | 状态 |
|---|---|
| 护甲减伤公式 / 饥饿 / 经验获取（挖矿 XP 等） | ✅ `research/01` §2/§3 有公式 |
| 挖掘时间公式 / 工具 tiers 表（木59/石131/铁250/钻1561/合金2031） | ✅ `research/01` §5/§8 |
| 攻击冷却 0.625s / 84.8% 全额伤害 | ✅ 有 |
| 燃料热值 / 合成配方**类型** | ✅ `research/01` §8 |
| 生物属性表（HP/伤害/速度/生成条件/特性） | ✅ `research/01` §10.2 相当完整 |
| 刷怪规则（光照0 / 24–128 格 / mob cap / 包生成） | ✅ `research/01` §10.3 |

**★ 缺口（本卡要补的六项）**：

| # | 缺口 | 严重度 | 证据 |
|---|---|---|---|
| 1 | **生物 AI 完全空白** | **高** | `research/01` 中"生物 AI"/"行为树"/"寻路" **均 0 处命中**；`docs/03 §6` 只有一句"生物 AI 行为树/目标栈" |
| 2 | **具体合成配方清单缺失** | **高** | 只有配方*类型*（有序/无序/烧炼…），没有"原木→木板""木板→木棍→工作台"这类实际配方 |
| 3 | **经验升级公式缺失** | 中 | `docs/01 §3` 写 `2L+7 / 5L−38 / 9L−158`，但 `research/01` **0 处命中** |
| 4 | **掉落物实体细节缺失** | 中 | `research/01` 中"掉落的物品" 0 处；`docs/01 §5` 只有"5 分钟消失" |
| 5 | **食物营养价值表缺失** | 中 | `docs/01 §3` 只说"按营养价值分层（需原创食物名/外观）"，无具体数值 |
| 6 | **被动生物（牛/羊/猪/鸡/鱼等价物）的繁育与掉落** | 中 | 属性表偏敌对，被动生物数据不足 |

### ⚠ 版本基准（PM 已拍板，不要调研这条）

- M2c 内容系统以 **`docs/01` 声明的 MC JE 1.21.x/26.x** 为准。
- 流体部分已用 1.19.2 实测校准过（用户确认 1.19.2 与 1.21.x 流体机制无差异），
  **那是流体，不影响本卡**。
- 若你调研中发现某数值在 1.21.x 有变更，请标注变更点即可，**不要**改变整体基准。

## ★ 范围（严格）

**做**：一份 `docs/research/11-mc-survival-systems.md`，覆盖上述六项缺口。

**不做**（看到了也不要动）：
- ❌ 不重复调研"已覆盖"表里的内容（护甲公式、挖掘时间、工具 tiers、刷怪规则等）
  —— 那些已有，重复做是浪费
- ❌ **不做百科**：每项给"实现够用"的精度即可
- ❌ 不写任何 C++ 代码，不改任何代码文件
- ❌ 不改 `docs/01` / `docs/03` 规格（PM 职权）
- ❌ 不改 `STATE.md`、不改记忆层
- ❌ 不调研 M4+ 的内容（附魔、酿造、锻造——`docs/01 §5` 已写明 M4 起）

### 精度要求（避免粒度失控）

| 缺口项 | 要什么精度 |
|---|---|
| **1 生物 AI** | ★ 重点。要能据此实现：AI 的**结构**（目标/行为如何组织）、**寻路机制**（如何找路、如何避障）、**感知**（视线/距离判定）、以及**几类典型行为**的具体参数（追踪距离、攻击间隔、游荡半径、恐慌/逃跑条件）。**不必**穷举 70+ 种生物各自的 AI |
| **2 配方清单** | 给 **M2c 首发进程**需要的配方即可：从原木到木板/木棍/工作台/熔炉/箱子，以及木石铁三档工具与盔甲的配方。**不必**列全 MC 上千条配方 |
| **3 经验升级** | 三个分段的公式 + 每段适用等级区间 + 死亡掉落规则 |
| **4 掉落物** | 实体的物理（重力/弹跳/阻力）、拾取判定、5 分钟消失、堆叠合并规则 |
| **5 食物** | 一张"食物 → 饥饿恢复 / 饱和度恢复"的表，覆盖首发进程的几种即可；说明饱和度的**作用机制**（为什么需要这个隐层） |
| **6 被动生物** | 牛/羊/猪/鸡/鱼等价物：HP、掉落物、繁育条件与冷却、跟随行为（小麦引诱等） |

## ★ 合规红线（`docs/04-legal-compliance.md`，触碰即返工）

1. **禁止粘贴任何反编译源码片段**（Java / 映射产物 / 逐行伪代码照抄）。
   机制可以用你自己的话描述，**算法结构可以画示意图，但不得搬运实现代码**。
2. **禁止逐字复制 Minecraft Wiki 文本**（CC BY-NC-SA，且 NC 禁商用、SA 传染）。
   只提炼事实与数值，用自己的话重写。
3. 允许保留的只有：**数学公式**、**数值/常量**、**机制的结构性描述**、
   **来源 URL + 访问日期**、你自己画的结构图。
4. 命名全部用本项目原创命名，不使用 MC 的内部类名/字段记号作为文档骨架。
5. 只记公开资料：Minecraft Wiki、公开技术文章。**不用** Yarn / MCP / Forge 反混淆产物。

## 取证手法（PM 已验证有效）

- 用 `curl -s -L "<url>?action=raw"` 拉 wiki 原文最稳（比网页渲染快、不易超时）。
  ⚠ **出网必须带代理**：`curl -x http://127.0.0.1:7890`（不带则 DNS 被劫持，SSL 报错）。
- WebFetch 在 wiki 上**容易超时**，优先用 curl。
- **access date 一律写 2026-09-17**（本次调研日期）。
- 引用一律用**规范 wiki URL + 访问日期 + 章节名**，
  **不要**用本地文件行号（行号会随 wiki 编辑漂移，URL 不会）。

## 允许触碰的文件/目录（白名单）

- **唯一产出**：`/Users/happy/Desktop/opencraft/docs/research/11-mc-survival-systems.md`
- 如需归档原始材料：可放 `/tmp`（**不进仓库**）

⚠️ **禁碰**：`STATE.md`、除上述文件外的所有 `docs/**`（含 `docs/01`、`docs/03`、
`docs/04`、`docs/05`、`docs/research/0*` 与 `10`）、`docs/tasks/`、
`engine/**`、`game/**`、`tests/**`、`cmake/`、`CMakeLists.txt`、`.github/`、`assets/`。

## 验收标准（逐条可执行）

1. **产出与合规**：文档存在、结构完整（覆盖六项缺口）、**通篇无反编译源码片段**、
   无逐字 wiki 段落。PM 会亲自读全文核对。
2. **数值可溯源**：每个关键数值/参数旁标注**规范 wiki URL（不含 google 跳转前缀）**
   + 访问日期。禁止无来源的数值。
3. **★ 生物 AI 讲透**（本卡最重要的一项）：读者能据它设计出 AI 结构并实现典型行为。
   含：目标/行为的组织方式、寻路机制、感知判定、典型行为的**具体参数**。
4. **配方清单可用**：覆盖"原木 → 木板/木棍/工作台/熔炉/箱子"以及木/石/铁三档
   工具与盔甲的配方，**可直接写成配方表**。
5. **未核实项显式标注**：任何你无法从公开资料确认的数值，标"待校准"，
   **不要**自行编造（本项目教训：发明数字比留空更糟）。
6. **不重复已覆盖内容**：不重复调研护甲公式/挖掘时间/工具 tiers/刷怪规则等已有项。
7. **P-001 合规**：未改动白名单外的任何文件。
   你的建议/新发现，一律以**建议表**写进调研文档内的「建议」章节，交 PM 落盘。
8. **报告**：落 `/Users/happy/Desktop/opencraft/docs/tasks/T-R2.report.md`，
   对话中**只输出简短版 + 该路径**，用 **txt 代码块**包裹（`docs/05` §2 规则 4）。
9. 完成后置 agentmemory action **`act_mu4fixof_c75f56fa541d`** 为 done。

## 已知风险与提示

- **这是调研卡，不是实现卡**：你的价值是把缺的信息补齐，不是把论文写长。宁可少而准。
- **纠正 PM 是被鼓励的**：若你发现某数值与 wiki 不符、或 PM 给的缺口判断有误
  （比如某项其实已有），**直接写在报告里**。本项目已多次由执行方纠正 PM。
- **别做成百科**：T-R1（流体调研）时就差点失控，靠卡面明确写"不必展开"才收住。
  本卡同样：**每项给"实现够用"的精度**。
- **合规**：描述机制时可以说"对齐基准 MC JE 1.21.x/26.x"，
  这是项目既有口径（`docs/01` 首行已如此表述）。
- **文档模板**：参照 `docs/research/10-mc-fluid-dynamics.md` 或
  `docs/research/07-physics-knowledge-base.md` 的头部格式
  （成文日期 / 状态 / 定位 / 合规声明 / 落地规则 + 目录）。

## 附：为什么这些缺口值得专门派一张卡

M2c 是"生存规则"全量（合成/工具/饥饿/战斗/生物/刷怪），
是本项目**内容密度最高**的一个里程碑。
带着缺口开工，会遇到"实现到一半发现不知道该怎么做"——
那比先花一轮调研贵得多。

尤其**生物 AI**：`docs/03 §6` 只写了一句话，而它是整个 M2c 里
**结构最复杂、最容易做错**的部分（`research/01` 里 0 处命中，完全依赖本卡）。
