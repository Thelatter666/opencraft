# T-B5 裁决 · 美术返工（Mossback 比例对齐原版牛 + Wretch 三倍加密）

> 裁决人：PM（第四任）　日期：2026-09-18
> 对象：分支 `task/T-B5-mob-art-rework` @ `a2711c7`（基线 = main `ba2f920`）→ **合并见本提交**
> 报告：`docs/tasks/T-B5.report.md`　PM 证据：`docs/qa/T-B5-2026-09-18/pm_verify/`
> **结论：通过。两条用户指令（三倍加密 / 比例对齐原版牛）与转发时追加的"明显面部特征"全部达成，
> 且在引擎内可见。§3 处理一条卡面自相矛盾。**

---

## 0. PM 独立复现

| 判据 | PM 独立结果 |
|---|---|
| 白名单 | 46 文件全在卡面 §3 内；`game/engine/tests/cmake` 零改动；**T-B1/T-B2/T-B2b/T-B3 目录零触碰**；blastbud 资产未动 |
| 干净检出 | 459/459（**外网全断下用 `FETCHCONTENT_SOURCE_DIR_*` 指主仓 `_deps` 只读拷贝完成**——新离线法，已写进证据 README） |
| 第四份实现读回 | **ALL PASS**（两件、含 B5 专项：腿高 7/外缘齐平/苔不越头/面格表面着色/四肢 2 格宽/第二色落同关节；四 md5 与报告全同） |
| 日志两向 | `2/3` + 两行 + atlas 60/63 + `[warning]/[error]`=0；移走 → `0/3`、0 |
| 暂停帧 | 苔背兽复现 **0**；模型 vs 占位 **20,294**（苔背兽）/ **19,834**（枯役，相机同位对） |
| **面部特征（转发追加需求）** | 引擎内实拍：苔背兽"黑眼窝+鼻点"可读；枯役俯仰单张 **2×2 眼窝+鼻+口** 全部可见 |
| 第三方 | MV 两件尺寸读数正确（开发者截图）；开发者 A/B 对照渲染与 PM 实机互相印证 |

**装置新账（本回合的坑与修法）**：HID 激活使**相机 yaw 随机漂 ±2px**（同场景两跑差 16.8 万像素、
整屏 bbox——差点被读成"模型画飞了"）。修法：`pair.sh` 用**无生物区对齐检测**，ratio=1.0 且
dx=dy=0 才接受，否则重拍（本轮第 4 次命中）。另确认**俯仰注入单张可用、跨跑不可复现**，
故只用于目检照，不进 A/B 矩阵。

## 1. 交付一验收：Mossback 比例对齐（用户指令 ②）

| 项 | 原版牛（官方 cow.geo.json） | v4 | **v5 实测** | 判定 |
|---|---|---|---|---|
| 腿高:躯干高 | 1.2 | 0.83 | **1.17** | ✓ |
| 四腿位置 | 身体外缘 | 内缩 1 格 | **x{0,1,5,6} 与躯干 x0..6 齐平** | ✓ |
| 苔丘 | —（我们自己的特征） | 3 层悬檐圆顶、**苔顶高于头** | **1 格薄盖、苔顶 13 = 头 13** | ✓ |
| z 跨度 | —（口径：=碰撞高/0.1） | 14 | **14** | ✓ |
| 派生比值 | 体长:体高 1.80 / 体宽:体高 1.20 | — | 1.83 / 1.17（±5% 内） | ✓ |

PM 实机目检：四腿变长、身变扁平、苔成薄盖，**"看得很怪"的比例问题消失**。

## 2. 交付二验收：Wretch 三倍加密 + 面部特征（用户指令 ① + 转发追加）

- 体素 **169 → 496（2.94×）**；四肢 **2 格宽**（PM 读回逐肢验证）、胸腔凹 2 格深、锯齿下摆、
  眉弓 + 下坠下颌；**面部 14 格**（2×2 眼窝 + 鼻窝 + 口）——全部"表面着色"判据通过。
- PM 实机：胸腔空洞/骨手/双腿可读；**俯仰单张里骷髅脸五处特征全可见** ⇒ 追加需求成立。

## 3. 裁决：卡面 §1.2.2 与验收判据互斥——**按验收判据执行（苔 1 层）**，PM 认账

卡面 §1.2.2 写"苔最多中部 2 格"，但 §1.2.1 与验收判据锁 **z 跨度=14**（腿 7 + 躯干 6 + 苔 1）。
两者不能同时成立。开发者按**验收判据**执行（苔 1 层）并上报——处置正确。
这是本轮第 2 次"卡面自相矛盾"（第 1 次：T-B2b §3.2.2 vs 白名单）；已在本卡变更记录追加更正行。

## 4. 四条"知悉"全部接受

① 高度预算把头锁在 z9..13（头必然比背线高 1 格）——**这正是原版牛的读法**（牛头也高于背线），接受；
② 躯干宽取 7 而非 8（5 宽的头需居中，奇数对奇数）——工程上正确的取舍，接受；
③ 枯役实体 1.0 格宽、手臂探出碰撞盒 0.2 格——与原版人形一致（牛头/角同样探出碰撞盒），接受；
④ 两处调色板改动（mossback 格 10 U0→O0、wretch 格 13/14→S3 双脚）——v2 机制的自然结果，接受。

## 5. 流程分

高。四处做得好：① 用户追加需求单独落一次提交、判据化（五官格四条件）；② 自曝并修掉自己工具里
"透视背面剔除写反"的 bug（症状=五官全不见，正是最容易被误读成"模型没做"的一类）；③ v4 旧件用
`git show <基线>` 取件、归档进本卡证据目录（既保对照又不碰历史目录）；④ 20 项证据自带索引 README。
**给 B4 的口径**：Blastbud 派发时"前两只行零回归"以**本卡后的新行**为基线。
