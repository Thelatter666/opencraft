# T-D8 裁决（2026-09-15，收口汇报后）

## 验收结论：**通过，已合入 main**

PM 独立验证（`git archive` 干净检出，从零构建）：configure 0 错误、构建 0 错误、
**ctest 203/203 全绿**（184 存量 + 19 新增）。白名单核对通过
（改动仅 `engine/physics/**` + `tests/**`；其余为 PM 自己提交的文档，非开发者改动）。

## 裁决 S-1：验收 #1「1 格高」与 `step_height = 0.6` 互斥 → **维持 0.6（选项 a）**

**采纳 (a) 维持 0.6。** 依据三条，且互相加强：

1. **与用户已定的 T-D14 方向自洽**：用户 2026-09-15 已裁决「对齐 MC：另立 Auto-Jump 卡」。
   Auto-Jump 正是 MC 用来处理 **1 格整方块**的机制（research/08 §2）。
   若把 step 改 1.0，Auto-Jump 就变得多余，且偏离原版——两个决策会互相打架。
2. **遵守总原则**：`docs/00`「对齐 MC JE」。MC 玩家 step 0.6 < 1.0，走不上整方块是**原版行为**。
3. **副作用**：改 1.0 会让绊线钩/压力板一类的低位机关失效（research/08 §5），
   且生物（原版 1.0）相对玩家的优势消失。

**验收 #1 措辞修正**：卡面 §7.1 的「1 格高（含 ≤0.6）台阶」改为「**≤0.6 的台阶**」——
该措辞是 PM 的错误（把用户原话里的「1 格高坎」直接抄进验收，未核对 0.6 的物理含义）。

**且需明确告知**：用户原始抱怨「被 1 格高坎卡住需手动跳」属 **MC 正确行为**，
处理方式不是放宽 step，而是 T-D14 的 Auto-Jump。

## 开发者发现的卡面缺陷 → 记功

开发者在 T-D13 开工前发现：**T-D13 卡面 §1 引用 `player_physics.cpp:617`
（`lifted.position.y += lift`）并称「T-D8 已实现」，但 T-D8 从未合入 main**
（main 上该文件仅 471 行、无 `step_height`、`game/` 两侧逐位相同）。

**这是 PM 的错误**：写卡面时读的是 T-D8 **worktree** 里的代码，却当作 main 写进了卡面，
并据此判定「根因已定位」。后果是 T-D13 在 main 基线上**无法复现**要修的现象。

**已修正**：本裁决后 T-D8 合入 main，T-D13 卡面的行号引用随之成立。
**教训入 lesson**：卡面引用代码行号前，必须确认该代码在**卡面指定的基线分支**上真实存在。

## 其余建议表处置

| # | 建议 | 裁决 |
|---|---|---|
| S-2 | docs/01 §2 补「步高 0.6（生物 1.0）」 | ✅ 采纳 |
| S-3 | docs/03 §6 补 `shape_top_at` 接缝说明 | ✅ 采纳 |
| S-4 | 新债：`on_ground` 的 1-tick 滞后 | ✅ 采纳（登记 T-D15） |
| S-5 | 新债：天花板夹持未高度感知 | ✅ 采纳（登记 T-D16，M2 形状卡） |
| S-6 | 新债：水中不跨步未对照 MC | ✅ 采纳（并入 T-D12 水卡） |
| S-7 | lesson：「最低增益」类排序性质须用变异测试验证 | ✅ 采纳（已入 lesson） |
| S-8 | lesson：严格重叠判定下候选查询须用扫掠盒 | ✅ 采纳（已入 lesson） |

S-7 尤其值得记：开发者自曝首版 4 个「最低增益」用例是**空约束**（改降序后 18 例仍全过），
补了判别用例并用变异测试证明其真有约束力。这种「主动检验自己的测试是否有效」应制度化。

## 给 T-D13 的基线裁决

**采纳选项 B 的变体：先合入 T-D8，T-D13 从新的 main 开出。**
理由：T-D8 已裁决通过且 203/203，合入后 T-D13 卡面的行号引用、实机复现、
基线计数（203）全部自洽，无需 scratch 环境的额外交代成本。
