# T-B2 裁决 · Mossback 体素模型（美术侧首只生物）

> 裁决人：PM（第四任）　日期：2026-09-18
> 对象：分支 `task/T-B2-mossback-model` @ `321c362`（基线 = 派发点 `3bd38dc`）
> 报告：`docs/tasks/T-B2.report.md`　双方证据：`docs/qa/T-B2-2026-09-18/`（含 `pm_verify/`）
> **结论：通过，合入 `73a37ef`。四项待裁事项逐条见 §2–§5。**

---

## 0. PM 独立复现（不采信自报，装置细节见 `pm_verify/README.md`）

| 判据 | 开发者宣称 | PM 独立结果 |
|---|---|---|
| 零代码改动 | 24 文件全在白名单 | `git diff --name-only main...HEAD` 24 文件逐项核对：无 `game/**`、无 `engine/**`、无 `tests/**`、无 CMake；卡面 `T-B2.md` 未被修改；风格指南仅 §9 + 变更记录追加 ✓ |
| 干净检出回归 | （美术卡不涉代码） | 457/457（另：合入后主树 457/457）✓ |
| 日志判据 | `1/3` + 期望行 + WARN=0；对照 `0/3` | 逐字复现；**加测第三向**：只移走调色板 PNG ⇒ `1/3` 且行尾无 `palette from`（回落 .vox 自带色板），WARN=0 ✓ |
| 模型真实性 | 引擎 672/1504 与其 Python 读回互证 | **PM 另写第三份独立解析器**（`pm_readback.py`）全项 PASS，含每条关节包围盒、z=0 层只含四腿（抗 y-up）、RGBA"第 e 条=索引 e+1"逐槽、PNG 格号=索引逐格 ✓ |
| 可复现性 | 重跑编码器 md5 相同 | md5 逐字节相同（f807283…/ba69f5a…）✓ |
| **上屏（报告 §6.3 自认缺口）** | 无 | **PM 补齐**：暂停帧装置复现性 0 px；模型 vs 两立方体 **84,827 / 20,496** px；模型 vs 空 **17,641** px；目检定稿图头/四腿/苔丘读得出（`pm_verify/pm_final_m7_mob.png` vs `pm_final_s4_standin.png`）✓ |

装置账（我的弯路如实记）：为躲暂停菜单试过**横移生成点**（落草阶、生物埋坡，弃）与
**暂停态注入俯仰**（鼠标未捕获、逐像素无变化，弃）；解 = 召唤距离 d=4.5 让 1.4 格高
生物的全身投影恰好落在菜单下缘与热栏上缘之间。已写进 `pm_verify/README.md` §3/§4，
**给 B3/B4 直接用**（人形 1.9 格高，届时按同一几何账重算，方向：加大 d 或接受脚部入热栏）。

## 1. 风格与合规

§8 自查十项判定成立（含三处"不适用"的理由）；原创性声明（红线 2/5）与 CREDITS 两行六字段齐；
**两条 CREDITS 行**按该文件"每资产一行"自身规则成立，卡面"追加一条"按单文件口径估的，予以更正。

## 2. 裁决一：「一关节一色 ⇒ 头画不了眼睛」——**批准扩约，另开引擎卡 T-B2b，B3 之前必须完成**

这正是 research/12 §4.3 预留的触发条件（"美术侧反馈不够用就扩"），且反馈来自**首只模型的
实际制作过程**，不是假想。定案：

- **契约 v2**：colorIndex `1..16 → 关节 (idx−1) mod 8`（9..16 = 各关节第二色）；
  `17..255 → 躯干组`。关节数、枢轴、draw call、解析器、`mobs: N/3` 判据行**全部不变**。
- 改动面：`mob_mesh.cpp::mob_joint_of_color` 一个函数 + 单测
  （存量"9+ = 躯干"断言**逐条 old→new 更新，卡面白名单显式许可**；新增 16/17 边界用例）。
- **mossback 资产迁移**：现用 9..16（躯干色）→ 17..24。稿件/编码器/调色板手稿改动
  机械（索引平移 8），md5 复现装置保证迁移可核。
- 已当场落实：风格指南 §9「待裁决：生物一关节一色」行改为已裁决指向本文件（随本提交）；
  `docs/tasks/T-B2b.md` 本回合计派发（见 §7 派发记录）。

## 3. 裁决二：模型长 1.40 > 碰撞盒 0.9（头尾各出 0.25）——**接受，不改**

这不是"与原版一致"的判定（无实机基准，不装懂），而是**设计接受**：视觉体与碰撞体解耦是
本项目既有约定，四足兽体长≈体高才读得成四足兽（报告 v1 的"站立方箱"迭代记录就是反面证据）。
打头判定与贴墙穿模是已知代价，登记：真人验收若被"头穿进墙"硌到，按**校准卡**先取原版实机
基准（同 T-F1 流体那条路）再议碰撞盒数值，不在美术卡里顺手改。

## 4. 裁决三：Goxel 口径——**收敛，PM 旧结论降级**

我此前"Goxel 已可用"的复核只证明了**进程起得来、渲染正常、画布是空的**（那张证据图现在
看恰恰是不加载的证据）。开发者四条路（CLI 位置参数 / CLI 导出 / `open -a` / File>Open）
全留痕 ⇒ 本机 Goxel 0.15.1 **收不发 .vox**。更正三处：卡面变更记录追加更正行（本提交）；
我的记忆文件同步改；**B3/B4 及以后的卡面不再写"用 Goxel 出 .vox"**，
"逐层字符稿 + 自写编码器"转正为**主路线**（已验证可复现，`docs/qa/T-B2-2026-09-18/tools/`
四件套照抄即可）。MagicaVoxel 若引入需下载（0.99.6.2 + Rosetta），不阻塞任何卡。

## 5. 裁决四：口径差异两条——**均接受**

- "头 z 高于躯干主体"按契约字面成立（11>9）；苔顶 13 > 头 11 是识别特征（低头吃草），
  两种口径的数都在 `inspect_selfcheck.txt`，处理透明，**记为设计意图**而非契约违反。
- 四条腿同色：接受。方位判据本来就不该依赖颜色（每标签包围盒表更硬），颜色预算省给躯干。

## 6. 通过判定与流程分

- 验收标准 1–5：**全部通过**（1/2 独立复现、3 逐数复算、4 由 PM 装置补齐、5 合规自查核过）。
- 流程分：高。三处卡面出入全部**主动申报**（Goxel、一关节一色、截图未拍到），
  其中两处推动了这个项目的格式演进；md5 宣称在报告里刻意不写死提交号（吸取 T-B1 悬空对象教训）。
- 给后续卡的三条复用产出（1 体素=0.1 格档位 / tools 四件套 / RGBA 错位坑注释）**收进 B3 卡面**。
