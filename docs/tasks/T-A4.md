# 任务 T-A4：第二批方块贴图（补齐 20/20）

里程碑：M2c（**美术总监**）　前置任务：T-A2（资产管线）、T-A3（美术规格 v1 + 首批 42 张）

> 卡面是派发时点快照，**正文不得修改**；修订一律追加「变更记录」。
> 状态唯一权威是 `STATE.md`，本文件不写状态。
> ★ **本卡由「美术总监」承担**（`docs/05 §1` 角色表）。

---

## 0. 为什么有这张卡

T-A3 交付了首批 **42 张**（14 个方块 × 3 面），已验收合入。
**剩余 6 个方块**仍走程序化回退——画面里现在是「14 个新贴图 + 6 个旧占位」混在一起。

本卡把剩余 6 个补齐，使**全部 20 个方块都有真实贴图**（20×3 = 60 张）。

**开工前必读**（本卡不重复其内容）：
- `/Users/happy/Desktop/opencraft/docs/art/01-style-guide.md`（238 行，**本卡的规格依据**；§9 已把第二批列为待办）
- `/Users/happy/Desktop/opencraft/assets/README.md`（86 行：目录布局、文件规格、回退语义、三条红线）
- `/Users/happy/Desktop/opencraft/assets/CREDITS.md`（证据链格式与固定字段）

---

## 1. 目标（一段话）

**为剩余 6 个方块各做三面贴图（18 张 PNG）落进 `assets/blocks/`，**
使启动日志从 `42/63` 变为 `60/63`，全部方块脱离程序化回退；
每张都有完整 CREDITS 证据链，风格与首批一致。

---

## 2. ★ 角色边界（与 T-A3 相同，不重复解释）

| 你**做** | 你**不做** |
|---|---|
| 美术资产：PNG 贴图、更新规格文档 §9 待办 | **C++ 代码**（含"用代码生成贴图"） |
| `assets/**`、`docs/art/**`、报告、证据目录 | `docs/0X-*.md`、`docs/03/04/05`、`STATE.md`、`engine/**`、`game/**` |

需要引擎侧变更请以建议表写进报告，由 PM 转开发者。

---

## 3. 交付物

### 3.1 第二批贴图（18 张）

六个方块，各 `_top` / `_side` / `_bottom` 三面：

| 方块 | 程序化主色（`atlas.cpp:46 base_color()`） | 现有程序化特征（可作为气质参考） |
|---|---|---|
| `cobblestone` | `{112,112,118}` | 4×4 格纹、每格随机明暗 |
| `gravel` | `{138,133,128}` | — |
| `sandstone` | `{200,185,145}` | 侧面有横向条带（4 行一循环） |
| `bedrock` | `{70,70,74}` | 两色随机噪点 |
| `snow_block` | `{226,230,236}` | 高亮、接近白 |
| `obsidian` | `{44,36,58}` | 深色 + 约 1/13 概率的高光点 |

⚠ **这些是"占位期间凑出来的值"，可参考气质、不受其数值约束。**
首批的做法是先看了这些原值、再决定沿用/重定；本卡**沿用同一套方法论**
（规格 §2.2 已为第二批预订了 S4/S5 等坑位，请按其执行）。

### 3.2 更新规格文档

- `docs/art/01-style-guide.md` §9 待办表：**第一批「第二批方块」一行标记完成**
- 若本卡新发现需要登记的后续项（如某个方块的特殊处理），**追加进 §9**，不要改正文其他节

---

## 4. 每张贴图的要求（与 T-A3 一致）

- **正好 16×16 PNG**（`assets/README.md` §2：不符会被拒绝并**静默回退**，不缩放）
- **朝向正确**：PNG 第 0 行 = 面的上沿
- **色板受 `docs/art/01-style-guide.md` §2 约束**（32 色限定 + 低饱和判定见 §3）
- **`CREDITS.md` 每条一行，六字段齐全**
- 与首批保持**同一世界的观感**（§4 构图语言、§5 三面关系、§6 alpha 约定）

### 4.1 ★ 合规三条硬规则（逐字见 `docs/05 §2`，本卡不再改写）

1. **AI 输入禁止含 MC 资产**——参考只能给**文字描述**，不得喂原版贴图/截图/官方美术。
2. **提示词不得点名 MC**——不得出现 "Minecraft-style" 等。
3. **证据链照旧**——AI 生成不豁免留痕。

> 首批 42 张是**全手绘**（未用 AI）。本卡**不要求**你效仿——用 AI 是允许的，
> 违规的是"喂 MC 资产"和"不留痕"。**如实申报即可**。

---

## 5. 允许触碰的文件/目录（白名单）

**允许**：
- `/Users/happy/Desktop/opencraft/assets/blocks/*.png`（**新建**，交付物）
- `/Users/happy/Desktop/opencraft/assets/CREDITS.md`（**追加条目**，字段固定、勿改表头）
- `/Users/happy/Desktop/opencraft/docs/art/01-style-guide.md`（**仅** §9 待办表的状态更新与追加项）
- `/Users/happy/Desktop/opencraft/docs/tasks/T-A4.report.md`（**新建**，报告）
- `/Users/happy/Desktop/opencraft/docs/qa/T-A4-2026-09-18/`（证据目录）

**禁碰**：除上述外**一切**。尤其：**任何 `.cpp`/`.hpp`/`CMakeLists.txt`**、
`assets/README.md`（既有条文不得删改）、`docs/0X-*.md`、`docs/research/**`、
`STATE.md`、`game/**`、`engine/**`。

---

## 6. 验收标准

1. **18 张落盘**：6 个方块 × 3 面，一张不少（PM 会按 `block_id×3+slot` 反解逐张核对）。
2. **★ 日志判据**：启动日志从 `42/63` 变为 **`60/63`**。
   ⚠ 这是判据，**不是**"看画面觉得变了"——文件名写错是**静默回退**，只看画面会误判。
3. **实机前后截图**：同机位两张（本卡前 42/63 / 本卡后 60/63），
   能看出这 6 个方块确实变了。美术不能靠单测（`docs/05 §3.1` 第 5 条）。
4. **逐文件规格**：每张**正好 16×16**（PM 会逐张读 IHDR 核）。
5. **CREDITS 完整**：18 条新增，六字段齐全；PM 会做**文件 ⇄ 条目一一对应**勾核。
6. **规格 §9 已更新**：第二批一行标记完成。
7. **合规自查如实申报**：是否用 AI、用什么工具、输入是否纯文字、是否点名过 MC。
8. **★ 不留过程垃圾**：`__pycache__/`、`*.pyc`、临时 PSD、中间稿**不得入仓**
   （T-A3 教训，已写入 `docs/05 §2`）。证据目录只收最终产物：截图、日志、复跑脚本**源码**。
9. **白名单外的已跟踪文件零改动**。

**报告**：落 `/Users/happy/Desktop/opencraft/docs/tasks/T-A4.report.md`；
对话里只输出**简短版 + 完整报告绝对路径**（txt 代码块），不贴全文。

---

## 7. 已知风险与提示

1. **`docs/05 §6` 硬规则**：美术卡同样须建独立 worktree。
   ```bash
   git worktree add /Users/happy/Desktop/opencraft_worktree/opencraft-ta4 task/T-A4-blocks-batch2
   ```
   **所有 Read/Edit/Write 用 worktree 绝对路径**，不要写主仓。
2. **启动方式**：`cd build && ./opencraft`；资产根命中 `<cwd>/../assets`。
   **先确认日志行**（`atlas: N/63 block tiles loaded`），再判断生效与否。
3. **不做热重载**：改完贴图必须**重启游戏**才看得到。
4. **回退是静默的**：文件名写错不报错，只会回退到程序化图案。
5. **不要试图对齐 MC 的外观**——包括"某个方块大概长那样"。这条比技术约束更重要。
6. **发现引擎侧问题不顺手修**（例如贴图对但渲染歪了）——写进报告建议表，由 PM 转开发者。
7. **本卡不写 `STATE.md`、不改 `docs/0X`、不写记忆文件**（`docs/05 §5`）。

---

## 8. 附注：本卡做完后

20/20 方块贴图齐全（`60/63`，另 3 格是 air 不产生瓦片）。
规格 §9 还登记了其他待办（树叶镂空、物品图标、开采裂纹、生物模型贴图），
**均不在本卡范围**，保持待排。

---

## 变更记录

| 日期 | 内容 | 依据 | 授权 |
|---|---|---|---|
| 2026-09-18 | 初版落盘 | 用户裁决「选 a：继续美术」；规格 §9 已列第二批待办 | PM |
