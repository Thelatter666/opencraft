# T-D13 裁决（2026-09-15，收口汇报后）

## 验收结论：**通过，已合入 main**

PM 在干净检出（`git archive`）从零验证：**225/225 全绿**（203 存量 + 22 新增）。
`engine/physics/**` 与 `tests/golden/` **确为零 diff**，白名单干净。

## 裁决 1：积分器改用解析解 → **批准，且记功**

卡面建议的半隐式欧拉在 **30 fps 发散**（ω·dt = 0.867 落在稳定域外，过冲 +8446 格、
终值 −9562），而 60/144 fps 正常——**正是卡面明令禁止的帧率不一致**。
开发者改用临界阻尼解析解（ζ=1.0，ω=26 rad/s），8 fps 起单调无过冲，与连续解差 ~1e-12。

**这是开发者纠正了卡面的错误建议**，且纠正得对：实测 30/60/144 fps 下
0.6 格过渡分别为 0.233 / 0.217 / 0.214 s，一致落在 0.15–0.25 窗口。

教训（S-4，采纳）：**卡面给的数值型实现建议须先做稳定性验算**，不能只给公式不给步长约束。
已入 lesson。

## 裁决 2：★ step-assist 实机不可达 → **确认属实，登记 T-D17（高优先）**

PM 独立复核确认开发者判断：

- `IBlockSource::shape_top_at` 默认实现 = 实心方块返回 **1.0**
- `game/` 与 `engine/voxel/` 的 `WorldSource` **均未覆盖它**（`git grep` 为空）
- ⇒ 世界唯一高差 1.0 > 玩家 step 0.6 ⇒ 候选集恒空 ⇒ **机制永不触发**
- 单测之所以 203/203 全绿：`physics_test::BoxWorld` **覆盖了** `shape_top_at` 才构造出 0.5 高差

**这意味着 T-D8 并未真正修复任何实机场景。** 我上一轮对用户说
「T-D8 修好了 ≤0.6 的高差」**不准确**，须更正：正确表述是
「step-assist 已实现且单测通过，但真实适配器未接 `shape_top_at`，实机暂不可达」。

**S-2 采纳**：对外（对用户）的预期管理按此口径，不得称"台阶已修复"。

登记 **T-D17**（`act_mu1ulil7_ac7dd414a8a6`，优先级 6）：随 M2 半砖/地毯/床内容卡
把 `WorldSource::shape_top_at` 接上真实顶面高度（半砖 0.5、床 ~0.5625、地毯 ~0.0625、
整方块 1.0），并补一条**可达性证据**。

## 开发者自我纠正的两处 → 记功

1. 滞后限幅初版取 0.6（= 步高，看似合理）：实测无约束滞后已达 0.567，仅剩 0.03 余量，
   会把 S 曲线压成线性跟随 → 改为 0.9（下界取实测 0.567、上界取潜行眼高 1.27，两侧留余量）。
2. 瞬接阈值初版 2.0 格会把普通快速下落误判为传送 → 按"被钳制帧内目标最大位移 0.784"
   重推为 20 格，并加守门单测。

这两处都是**"看似合理的取值被实测推翻"**，且开发者主动交代而非事后美化。

## 建议表处置

| # | 建议 | 裁决 |
|---|---|---|
| S-1 | 登记 T-D17（WorldSource 未接 shape_top_at） | ✅ 采纳，优先级 6 |
| S-2 | 对外勿称"台阶已修复" | ✅ **采纳，并已更正我对用户的上一轮表述** |
| S-3 | docs/03 §6 补相机 Y 弹簧解耦参数 | ✅ 采纳 |
| S-4 | lesson：卡面数值建议须先做稳定性验算 | ✅ 采纳（已入 lesson） |
| S-5 | lesson：「机制已实现」≠「机制可达」 | ✅ 采纳（已入 lesson，含判别手法） |
| S-6 | 新债：kEyeStanding/kEyeSneaking 在 :879 与 :1107 重复计算 | ✅ 采纳，登记 T-D18（低） |

## 用户视角的净收益（如实表述）

本卡**确实**消除了垂直突变的瞬移感，但作用于**所有**垂直突变，而不只是台阶：
- 潜行姿态切换 0.35 格：旧相机一帧跳 0.350000 → 新相机同帧 0.000063，约 23 帧单调收敛
- 跳跃弧线：单帧最大 |ΔY| 0.1218 → 0.0865；|ΔY|>0.1 的帧数 5 → 0

台阶场景的实机复验待 T-D17。
