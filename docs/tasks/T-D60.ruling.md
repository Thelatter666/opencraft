# T-D60 裁决 · 合成与工具 tiers（第五任 PM）

> 对象：分支 `task/T-D60-crafting-tiers` @ `cb25475`（基线 = main `379424d`）
> 日期：2026-09-19　裁决：**通过，合入**
> 证据：`docs/qa/T-D60-2026-09-19/`（开发者）、`docs/qa/T-D60-2026-09-19/pm_verify/`（PM）
> 卡面：`docs/tasks/T-D60.md`

---

## 0. 结论

七裁全部落地，存量 492 用例名零缺失 + 新增 37，禁碰项零碰（`protocol.hpp`/`player_life.hpp`/
mob 侧 0 diff = **C-2"合成客户端本地零协议改动"的直接证据**），实机全链日志闭合。
**四条裁决全落盘（§2），其中两条是 PM 卡面自身的错误、由实现方抓出——均按实现口径采纳。**

| 项 | 值 |
|---|---|
| 测试 | 干净检出 `cb25475`（离线 deps）**529/529**；492 零缺失 + 37 新增 |
| FNV 冻结表 | **机器验证平移关系**：0..62 逐字节相同 ✓、新 63..65 是新像素 ✓、**新 66..75 == 旧 63..72**（crack 平移 3）✓、背景格 8→5 值不变 ✓——"推导式更新"是逐字节可证的事实 |
| 实机 | `atlas: 60/66` + `crack tiles at 66` + `mobs: 3/3`、三份日志 WARN/ERROR = 0；全链 `crafted(2×2)` → `placed assembly_bench (7,143,-164)` → `opened a 3x3 surface` ×5 |
| 采集等级口径 | PM 重算十行门控全对（`kNoTool=-1` + 需求=Tiers 档位） |

---

## 1. PM 独立读回（非转述报告）

1. **需求表**（`mining.cpp` `requirement_of`）：石族/煤 **0**、铜/铁 **1**、金/钻 **2**、黑曜石 **3**、
   其余 `kHandHarvestable`、不可破坏 `kUnmineable`——**每条都有依据注释**（石族=0 的理由引
   §4.1 权威算例与进程链；金=2 是 PM 保守值并明写"不要擅自改成 1"；**铜=1 是 research/01:138
   根本没列的档**，实现按 §9 Tiers 自行补齐并说明——这是实现方对 research 缺口的正当补全）。
2. **挖石转换**：落在 `ItemRegistry` 的 `set_block_drop(stone → rubble_rock)` 映射层（一行数据），
   `spawn_item_drop` 本体零改动——比在掉落函数里加分支干净。
3. **配方引擎**：`direct || mirrored` 两向尝试、垂直镜像不在尝试集（`recipe_registry.cpp:112`
   注释明说）；"匹配两条配方即拒绝不猜"（`a grid that matches two recipes is refused`）。
4. **门控**：`ui_open` 每 tick 读一次、`submit_world_verb` 收口全部八种动词（专测逐动词断言
   "屏幕开着 NOTHING is submitted"，且先证明屏幕关着请求确实能到达——双向有分辨力）。
5. **光标堆**：左键整堆拿起/放下/并/换、右键半堆（向上取整）/放一个、"装不下的关闭被拒绝且
   零变化"（`a close that would not fit is REFUSED`）、关闭时光标**与网格**一并归还（S-4 ②）。

---

## 2. 四条裁决

| # | 裁决 |
|---|---|
| **S-1** ★ 采集等级口径 | **采纳实现口径，C-6 字面作废**。卡面四处互斥（"0=徒手档" vs "Tiers 木 0" vs "石族需求 1" vs §4.1 表）是 **PM 写卡错误**；实现收敛为：工具档照 §9 Tiers 原样（木 0/石 1/铁 2/钻 3）、**徒手 = `kNoTool` = -1**（表外）、需求 = 同一刻度的最低档位。PM 独立重算十行门控**全对**，且 §4.1 五行与"木镐→圆石→石镐"进程链全部成立、契约⑦保持单一比较。**不采纳"坚持字面值"**——那需要改写 §4.1 并折断进程链。 |
| **S-2** §4.1 第 4 行笔误 | **确认是 PM 的算术笔误**（铁矿硬度 3.0：3.0×1.5/4 = **1.125 s**；0.5625 是石头行的数）。实现按 1.125 s（23 tick）钉死正确。报告更正、卡面不追溯（正文不可变）。 |
| **S-3** 关闭路线 | **接受 E/Esc 关闭**，C-1 的"右键工作台关闭"括注作废——它在 C-3 门控下**逻辑上不可达**，实现者还实测抓到"打开 3×3 的那次右键下一帧把界面关掉（0 ms）"的真缺陷并回避。移除该路线是正确工程判断。 |
| **S-4** 两处补全 | ① 物品**净增 6**（5 内容 + `assembly_bench` 方块形态物品行）：契约②的"5"没算方块形态行——没有它"方块可放不可得"，逻辑必要，卡面 C-5 表里本就有它作产物，**数字与表不一致是我的疏漏**。② 关闭时**网格内容**与光标堆一并归还：卡面只写了光标堆，网格归还防丢料，正当补全。两者均采纳。 |

---

## 3. 已知问题的处置

| 项 | 处置 |
|---|---|
| canHarvest 实机缺证（§5.4 第 2 条 + 木镐挖石半条） | **接受单测覆盖，不阻塞**：§4.1 五行被逐 tick 钉死（150/23/12/300 tick），且门控语义已由"徒手挖石 7.5 s 无掉落"单测与实机启动日志夹住。实机补验随下一张内容卡顺带（本裁决登记，不新开债）。 |
| 实机会话有真人输入污染 | **接受时间对齐归因法**：`injection_timeline.txt` 把关键行与注入时刻对齐（0.3–0.5 s），不采"我点了所以是我的"；未编排却发生的合成/放置只列为重复观测不作判据——这是诚实且可复核的处置。★ 该手法已回写 `docs/05 §3.1`（随本裁决提交）。 |
| 新方块/石档/木棍无贴图 | 程序化灰与扁平占位色（`item_tint` 已给三族可分辨的色），美术侧后续；`atlas: 60/66` 的缺口从此是 6（63..65 bench 三面 + 既余 3）——分母口径随卡面契约⑨。 |
| 耐久不做 | 维持 T-D61（本轮已登记）。 |

---

## 4. 排卡延伸（PM 落 STATE）

1. **进程线闭环已通**：木镐→圆石→石镐→铁矿可挖（石镐），但**铁档工具/盔甲仍被熔炼卡挡住**
   （铁矿石挖得出、烧不成锭）⇒ 下一卡候选明确：**熔炼（熔炉 + 燃料 + 铁锭）**，或按 M2c 顺序
   先做饥饿。熔炼卡同时解锁铁工具与铁盔甲（15 点）。
2. 启动包收回剑/锹已兑现（C-7）——真人验收时开局无剑是预期。
3. 美术侧待办（不阻塞）：`assembly_bench` 三面贴图、`timber_stick`/石档四件物品图标。

---

## 5. 证据清单（`docs/qa/T-D60-2026-09-19/pm_verify/`）

| 文件 | 内容 |
|---|---|
| `ctest_529_clean.log` / `configure_offline_deps.log` | 干净检出 `cb25475` 529/529 |
| `test_names_baseline_492.txt` / `test_names_529.txt` | 用例名集合原始数据（零缺失 + 37 新增） |
| `README.md` | 白名单核对 / FNV 平移机器验证 / 实机判据复核 / S-1 口径十行门控重算 |
