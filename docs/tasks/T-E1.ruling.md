# T-E1 裁决（2026-09-17）

> 依据：开发者报告 `docs/tasks/T-E1.report.md` + PM 独立复现。
> ⚠ PM 复现时用**分支**做干净检出（见"流程偏差"）。

---

## 验收结论：**通过**

| 项 | 结果 |
|---|---|
| 干净检出（分支 `task/T-E1-entity-layer`，Release 从零 configure+build） | **359/359** ✓（324 + 35 新增） |
| 冻结项 `engine/physics/**` | **零 diff** ✓ |
| `IAuthority` 五动词 | 零改动；`protocol.hpp` 仅 +19 行（新增 `PickUp`）✓ |
| 待校准项 | 全部写进代码注释：`restitution = 0.0`、着地摩擦 `0.98 × S`，并注明来源见 T-D36 ✓ |
| 实机 | 挖方块 → 掉落物可见 → 消失 → 快捷栏计数 32→33 ✓ |

### ★ 实机取证抓到的真缺陷（记功）

**掉落物渲染模型矩阵把立方体压到地面以下**（`T(-0.5)` 应为 `T(-0.5 × 边长)`）
⇒ 画面里根本看不见掉落物。

而"实体存在 / 物理正确 / 拾取正确"**全部正常**，单测全绿——
**单测覆盖不到渲染，这条只有实机能抓到。**

他的判据很漂亮：**相机静止时两帧逐像素完全相同**（说明画面里没有掉落物在动）。
这个判据值得记入 `docs/05`。

⇒ 这与 T-I2 那次"求值顺序崩溃"是同类：**单测结构上测不到的缺陷，靠实机取证兜住**。

---

## 裁决 1：**更正 T-D36 ③ 笔误** —— 采纳，已当场改

他指出 STATE.md 的 T-D36 ③ 写 `0.91 × friction`，但那是**玩家**的数；
掉落物自身水平阻尼是 **0.98**，卡面与 `research/11 §4.1` 都要求 `0.98 × friction`。

**我的笔误。** 已当场修正 STATE.md，并在债务行留痕说明更正来源。

> 教训：0.91 与 0.98 在这几张卡里反复出现（玩家 vs 掉落物），
> 抄数值时**必须确认主体是谁**。

## 裁决 2：着地摩擦一开，掉落物只比玩家多滑约 10% —— **登记待校准，维持现状**

他发现：启用着地摩擦修正后，普通方块上掉落物只比玩家多滑 ~10%，
而 `research/11 §4.1` 说的"滑很远"只在**空中/冰面**成立。

**裁决：维持当前实现（0.98 × S），登记为 T-D36 的实机校准项。**
理由：这是"文档描述 vs 实测观感"的差异，不是实现错误；
且我们已按"待校准"标注，未编造数值——符合规矩。

⇒ 补进 T-D36 清单：⑤ 着地摩擦修正启用后的实际滑行比例（文档称"很远"，实测普通方块仅多 ~10%）

## 裁决 3：合并盒 0.5×0.25×0.5 相对基准未定 —— **采纳"先按字面，标注待校准"**

文档给了尺寸但没说基准（字面体积 vs 外扩 0.5）。
**裁决：先按字面体积实现，代码注释标注待校准**，并入 T-D36。

## 裁决 4：拾取是否也要常驻日志 —— **授权第二条**

本卡已有一条常驻日志（挖方块产掉落物）。
拾取日志对排查"东西去哪了"很有价值，**予以授权**，但要求：
- 仅在拾取**成功**时打印
- 格式与既有日志风格一致

## 裁决 5：实体不落盘 —— **登记，M2c 内解决**

当前：区块卸载只冻结不释放；重启后实体全失。
**登记为 T-D38**（中优先）——M2c 做生物时必须解决（否则重启后生物全没）。

## 裁决 6：无水中浮力 —— **登记，低优先**

掉落物在水中的浮力未做。登记 **T-D39**（低），随流体视觉卡一并处理。

## 裁决 7：碰撞代码与 `engine/physics` 重复 —— ★ **采纳，且优先级提高**

他指出：实体碰撞代码与 `engine/physics` 存在重复，
建议下次开物理层时抽公共 `sweep_move`，**否则生物卡会再造第三份**。

**裁决：采纳，且这是本卡最有前瞻性的一条建议。**
⇒ 登记 **T-D40**（中高），**排在生物卡（T-M2）之前**：
抽公共 `sweep_move`，避免第三份碰撞代码。

> 这与我们一直在避免的"双重真相源"是同一个问题。
> 开发者主动指出并给出了正确的解决时机（生物卡之前），记功。

## 裁决 8：物品注册表上收权威侧 —— **认可**

他将 `ItemRegistry` 的世界归属定在权威侧（世界决定方块掉什么），
理由是"权威侧决定方块掉什么"更合理。

**裁决：认可这一处归属变更。** 与 T-A1 确立的"权威侧持有世界状态"一致。

## 裁决 9：建议入 `docs/05` 的取证教训两条 —— **采纳**

（"相机静止两帧逐像素相同"判据、渲染类缺陷单测覆盖不到）

⇒ 已写入 `docs/05 §3.1`（见下方"当场落实"）。

---

## 当场落实（不重蹈"裁决写了没做"）

- [x] 更正 T-D36 ③ 笔误（0.91 → 0.98）
- [x] 登记 T-D38（实体不落盘）/ T-D39（水中浮力）/ T-D40（抽公共 sweep_move）
- [x] T-D36 补第 ⑤ 项（着地摩擦滑行比例）
- [x] 取证教训写入 `docs/05 §3.1`

---

## ⚠ 流程偏差（记录，不责怪）

**开发者把报告与证据提交到了 main（`824c85e`），但实体层代码留在分支上未合并。**
main 上因此只有文档、没有代码——PM 首次按 `git archive 824c85e` 做干净检出时，
检出的是**没有实体代码**的树（跑出来会是 324 而非 359）。

**PM 已自行修正**：改用分支 `task/T-E1-entity-layer` 做干净检出，并在本轮完成合并。

> 规则重申（`docs/05 §6`）：**代码与报告都应在分支/worktree 上**，
> main 只由 PM 在验收后合并。调研卡虽只加文档，同样应建 worktree
> （T-R2 那次也是直接落主仓，这次又发生一次 —— 已记入 lesson）。

## 记功

1. **实机抓到渲染缺陷**，且判据设计得干净（相机静止两帧逐像素相同）；
2. **指出 PM 的债务笔误**（0.91 vs 0.98）；
3. **前瞻性建议**：碰撞代码重复 + 明确"生物卡之前必须抽公共 sweep_move"——
   这条避免了第三份碰撞代码；
4. 待校准项全部如实标注，未编造；
5. 报告中说明了实体存储结构的**选型理由**（不引 EnTT、不用 unordered_map 的原因）。
