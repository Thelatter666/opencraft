# T-D46 裁决 · 战斗基础：玩家侧无敌帧 / 护甲 / 击退（第五任 PM）

> 对象：分支 `task/T-D46-combat-basics` @ `acfc500`（基线 = main `e322a77`）
> 日期：2026-09-19　裁决：**通过，合入**
> 证据：`docs/qa/T-D46-2026-09-19/`（开发者）、`docs/qa/T-D46-2026-09-19/pm_verify/`（PM）
> 卡面：`docs/tasks/T-D46.md`

---

## 0. 结论

八条契约全过，存量 459 用例名**零缺失** + 新增 18，`damage_mob` 判定块零改动，白名单零越界，
实机日志与权威算例吻合。**裁决 S-1 至 S-9 全部落盘（§2），新登记债务两条（T-D57/T-D58）。**

| 项 | 值 |
|---|---|
| 测试 | 干净检出（`git archive acfc500`，离线 deps）**477/477**；用例名 459 零缺失 + 18 新增（`comm -23` 为空） |
| 契约⑦ | `mob_sim.hpp` 的 diff 仅两处：新增 `knock_back()`（`damage_mob` 之后）+ MeleeHit 事件 position；**525-531 判定块无 hunk 触及** |
| 白名单 | 25 文件 = 源码 7 + 测试 4 + 证据 13 + 报告 1；STATE/卡面/规格/`protocol.hpp` 分支侧零改动 |
| 实机 | 空手 3.0/击 → 窗口吞击 7 条（带剩余 tick 与 last 读数）→ 穿甲 **2.3**/击；PM 重算 3.0×(1−22%)=**2.340** 吻合；Blastbud 自爆 49→**46.256** 与报告 46.3 吻合 |

---

## 1. PM 独立读回（逻辑卡的四路，非转述报告）

1. **无敌帧**：`player_life.hpp` 的 `settle_hurt` 与 `damage_mob` 同构（≤ 免疫 / 更高补差值），
   `kHurtInvulnerabilityTicks = 10` **int**；`tick_hurt_window` 每 tick 递减、先于事件结算
   （与 `advance_timers` 同相对次序）。★ **互指注释双向成立**：player 侧 "THIS IS damage_mob'S
   RULE, COPIED"、mob 侧 `knock_back` "Keep the two in sync"、`player_state.hpp` 亦指回 ——
   **卡面 C-1 的"不得静默分叉"要求达成**。
2. **护甲**：`armor_reduction` 逐字转写 `research/01:54-61`；`equipped_armor` **实时读四格不缓存**
   （卡面 §7.5）；`timber_*` = 1/2/3/1 韧 0（`item_registry.cpp` 常量 + 注释引 C-3），
   且注释明写"**不在此登记铁档**"（C-3 的另一半）。
3. **豁免**：`bypasses_armor` 表（Fall/Suffocation/Void/Starvation）⇒ 契约⑤；摔落路径显式走
   `damage_after_armor(Fall)`（绕甲但**可测**——"绕甲"是表项不是"没调"）。
4. **击退**：两侧同构（`knock_back` / `push_away`），`+=` 注入、与 `item_sim.hpp` 掉落物初速同构；
   **死亡态同门**：`world_tail` 里 `live = may_act(ctx.life)`，尸体不推进窗口、不结算、不被推
   （卡面 §7.7 要求的"被同一早退点挡住"达成，且比卡面要求做得更细——T-D45 的早退在 `run_tick`，
   本卡在 `world_tail` 里又对"活着才推进"加了一层，两层同源于 `may_act`）。

---

## 2. 九条裁决（对报告 §7 逐条）

| # | 裁决 |
|---|---|
| **S-1** ★ 盔甲不可达 | **接受为内容缺口，登记 T-D58**。开发者上报属实（PM 现数：`inventory_wiring.hpp:289-290` 四件在主背包 14..17；`equip`/`GLFW_KEY_E`/`swap`/`transfer` 多检索式均无装备途径）。**备选方案（挪初始装备到 36..39）不采纳**——那会让玩家出生即穿甲，改变 T-I2 的初始装备语义；"穿上"是装备界面卡的职责。⚠ **PM 自账**：卡面 §5.3 要求"产品码实机穿甲"，**发卡时未核到装备途径不存在**——这是 T-D17「内容缺口」教训的镜像（这次是**验收判据不可达**）。开发者的处置正确：钩子只写库存格，**伤害仍由交付代码计算**，且 README 写明装置能证明什么。本卡不返工。 |
| **S-2** MeleeHit position 语义 | **接受**（攻击者位置 = 击退方向所必需；原语义"落点"对玩家无用处）。protocol.hpp 三处过时注释**由 PM 当场落盘**（ActorEvent::position / kPunchDamage / Attack 枚举），注释级零行为。已知代价（报告 §8）：历史日志行的坐标语义不可跨版本按列比对——接受。 |
| **S-3** 窗口比原始伤害再套甲 | **接受**（原版 `LivingEntity.hurt` 次序）；代码注释已写明。 |
| **S-4** 摔落不吃窗口 | **接受**（卡面 §1.1 指示，且开发者正确指出"改它会动 T-D45 冻结的摔落数字"）。与原版的偏差**登记 T-D57**（待校准，不是缺陷——无原版基准，不得声称"与原版一致"）。 |
| **S-5** 窗口 off-by-one 读法 | **裁定：开发者的读法正确，卡面无矛盾**。"含命中那一 tick 共 10 tick"下，卡面 §5.2 #3 的"第 10 tick 仍免疫、第 11 tick 正常结算"成立（命中 tick = 第 1 tick）；且与 `damage_mob` 同构（其既有测试"步进 11 tick 避开窗口"同读法），mirror 用例把两侧锁死。**不改**。 |
| **S-6** 击退 `+=` 不减半 | **接受**（C-4 建议口径即"直接加进 velocity"）。与原版"先减半再叠加"的差异**并入 T-D57** 待校准；对外不得声称击退已对齐原版。 |
| **S-7** protocol.hpp 字样 | **已由 PM 落盘**（同 S-2 的一张注释改动）。 |
| **S-8** 皮革档强度 | **接受"无异议"**：49→46.3 仍致死、近战困难 5→6 击，不改战斗节奏定性；"更有感"属铁档（C-3 禁止本卡引入）。 |
| **S-9** md5 判据失效 | **接受并升级为方法论**：逻辑卡的"取证=交付"判据改用 **`git rev-parse HEAD` + `git status --porcelain` + 探针词计数**（二进制 md5 含随机 `LC_UUID`，重链即变）。已入 PM 记忆与 `docs/05`（随本裁决提交）。 |

---

## 3. 新登记债务

| ID | 内容 | 来源 |
|---|---|---|
| **T-D57** | **战斗侧与原版的两处已知语义差异（待校准，非缺陷）**：① 摔落伤害不吃受击无敌帧窗口（原版吃；改它会动 T-D45 冻结的摔落数值，须与原版基准一起裁）② 击退是 `+=` 叠加，原版先减半既有速度再叠加。均无原版基准，**对外不得声称"击退/无敌帧已对齐 MC"** | T-D46 裁决 S-4/S-6 |
| **T-D58** | **盔甲无装备途径**（内容缺口）：四件 `timber_*` 在初始背包 14..17，无装备键/背包界面，护甲数值当前只能靠钩子取证。由"背包/装备界面"卡关闭（同 T-D53 的"无获得途径"族） | T-D46 裁决 S-1 |

---

## 4. 报告两个回答的采信与延伸

- **空手杀 Wretch = 20 下 / 12 秒**（12 tick 冷却 × 20 击 = 240 tick；每击都落在 10 tick 窗口外故全额）。
  采信。⇒ **PM 排卡结论**：玩家对生物手感偏软的根因是"徒手 1.0 是全表唯一伤害源"，
  **"工具伤害/攻击充能"卡的优先级应高于原计划的"合成+工具 tiers"里的非工具部分**（见 STATE 5l 更新）。
- **ItemDef 改法优于旁挂表**：采信（三条理由成立，38 条全表零运行时开销）。

---

## 5. 证据清单（`docs/qa/T-D46-2026-09-19/pm_verify/`）

| 文件 | 内容 |
|---|---|
| `ctest_477.log` / `configure_offline_deps.log` / `build_clean.log` | 干净检出三条（离线 deps 法） |
| `test_names_baseline_459.txt` / `test_names_477.txt` | 用例名集合（⚖ 零缺失判据的原始数据） |
| `README.md` | 白名单核对 / 集合 diff / 契约⑦ diff 摘录 / 权威算例 PM 重算 |
